export enum PropertyType {
  SALE = 'sale',
  RENT = 'rent',
}

export enum PropertyStatus {
    AVAILABLE = 'available',
    RENTED = 'rented',
    SOLD = 'sold',
    PENDING = 'pending',
    unavailable = 'unavailable'
}